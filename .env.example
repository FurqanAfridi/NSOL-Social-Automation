# NSOL Social Media Automation - Environment Variables Template
# =============================================================
# This file lists all the environment variables required to run the n8n workflow.
# 
# INSTRUCTIONS:
# 1. Copy this file and rename the copy to '.env'
# 2. Fill in your actual values for each variable.
# 3. NEVER commit the real '.env' file to GitHub! (It's in .gitignore)
# 4. Load these variables into your n8n instance.

# Google Sheets Configuration
GOOGLE_SHEET_ID="your_google_sheet_id_here"

# Google Drive Configuration (optional)
GOOGLE_DRIVE_ID="your_google_drive_id_here"
GOOGLE_DRIVE_FOLDER_ID="your_google_drive_folder_id_here"

# n8n Credential References
N8N_GOOGLE_SHEETS_CREDENTIAL_ID="your_google_sheets_credential_id"
N8N_GOOGLE_DRIVE_CREDENTIAL_ID="your_google_drive_credential_id"
N8N_GEMINI_CREDENTIAL_ID="your_gemini_credential_id"

# The base URL of your n8n instance (important for OAuth callbacks if not running on localhost:5678)
N8N_HOST="localhost"
N8N_PORT="5678"
N8N_PROTOCOL="http"
