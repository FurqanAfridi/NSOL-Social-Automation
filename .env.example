# NSOL Social Media Automation - Environment Variables Template
# =============================================================
# This file lists all the environment variables required to run the n8n workflow.
# 
# INSTRUCTIONS:
# 1. Copy this file and rename the copy to '.env'
# 2. Fill in your actual values for each variable.
# 3. NEVER commit the real '.env' file to GitHub! (It's in .gitignore)
# 4. Load these variables into your n8n instance.

# Google Sheets Configuration
# -------------------------------------------------------------
# The ID of the Google Sheet that will act as the database.
# Find it in your sheet's URL: .../spreadsheets/d/[THIS_IS_THE_ID]/edit...
GOOGLE_SHEET_ID="your_google_sheet_id_here_123abc"

# Google OAuth 2.0 Credentials (for Sheets & Drive API access)
# Obtain these from your Google Cloud Console project.
# Create an OAuth 2.0 Client ID for a "Desktop Application".
N8N_GOOGLE_SHEETS_OAUTH_CLIENT_ID="your_google_oauth_client_id_here.apps.googleusercontent.com"
N8N_GOOGLE_SHEETS_OAUTH_CLIENT_SECRET="your_google_oauth_client_secret_here"

# It's common to use the same OAuth client for both Sheets and Drive.
# If you use different clients, you can create separate variables.
N8N_GOOGLE_DRIVE_OAUTH_CLIENT_ID="your_google_oauth_client_id_here.apps.googleusercontent.com"
N8N_GOOGLE_DRIVE_OAUTH_CLIENT_SECRET="your_google_oauth_client_secret_here"

# Google Gemini API Key
# -------------------------------------------------------------
# API Key for the Google Generative Language API (Gemini).
# Ensure the API is enabled in your Google Cloud Console and the key is restricted to it.
N8N_GEMINI_API_KEY="your_gemini_api_key_here_abc123"

# Optional: n8n Configuration
# -------------------------------------------------------------
# You can set a generic encryption key for n8n here.
# n8n will use this to encrypt your credentials in its database.
N8N_ENCRYPTION_KEY="your_secure_encryption_key_here_min_16_chars"

# The base URL of your n8n instance (important for OAuth callbacks if not running on localhost:5678)
N8N_HOST="localhost"
N8N_PORT="5678"
N8N_PROTOCOL="http"
